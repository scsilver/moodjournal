<%= form_for(@entry) do |f| %>
  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% @entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content, :cols => "45", :rows => "10", id: "entry"%>
  </div>
  <div class="actions">
    <%= f.submit 'Submit',type: "submit",:class => "btn btn-primary", id: "btnClick" %>
    <%= link_to 'Back', entries_path, class: "btn btn-default" %>

  </div>
<% end %>
